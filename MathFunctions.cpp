#include <bits/stdc++.h>
// #include <cmath>
using namespace std;
int main()
{

    double x = 3;
    double y = 4;
    double z;
    double D;
    int of;
    int notof;
    int notof2;
    int notof3;







    

    // Useful Math functions
    
    z = max(x, y);                 // max value finder
    D = min(x, y);                 // min value finder
    double something = pow(10, 4); // power funciton
    double another = sqrt(100);    // square root function
    of = abs(-69);                 // absolute value [positive]
    notof = round(63.14);          // round to nearest
    notof2 = ceil(63.14);          // round up
    notof3 = floor(63.14);         // round down











    cout << something << endl;
    cout << another << endl;
    cout << z << endl;
    cout << D << endl;
    cout << of << endl;
    cout << notof << endl;
    cout << notof2 << endl;
    cout << notof3 << endl;

    return 0;
}