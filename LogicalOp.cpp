#include <bits/stdc++.h>
using namespace std;

int main()
{

    //  And Operator : //"&&" => check if two conditions are true
    // Or Operator : //"||" => check if at least one of two conditions are true
    // Not Operator ://"!" => reverses the logical state of its operand

    return 0;
}


